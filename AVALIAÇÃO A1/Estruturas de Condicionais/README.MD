# 📂 Estruturas de Condicionais

Esta pasta contém as 12 atividades solicitadas nos slides do módulo, demonstrando o uso de estruturas de controle de fluxo (`if`, `elif`, `else`).

## ✔ Critérios de Avaliação Atendidos:
* Uso correto de `if`, `elif`, `else`.
* Entrada e saída de dados (`input()` e `print()`).
* Clareza no código e comentários.

## 📝 Detalhamento das Atividades

| Arquivo | Tarefa Realizada | Lógica e Estrutura Condicional |
| :--- | :--- | :--- |
| **Atividade01.py** | **Cálculo de Módulo (Valor Absoluto)**. | Verifica se o número `n` é maior ou igual a zero. Se for negativo, aplica `modulo = -n` para obter o valor absoluto. |
| **Atividade02.py** | **Intervalo em Ordem Crescente**. | Compara os dois limites (`a` e `b`). Usa o `if/else` para definir `limite_inferior` e `limite_superior` e exibir o intervalo. |
| **Atividade03.py** | **Raízes da Equação do 2º Grau (Discriminante)**. | Calcula o **Delta** (`delta`) e a condicional verifica se `delta < 0` para classificar as raízes como **COMPLEXAS** ou **REAIS**. |
| **Atividade04.py** | **Cálculo de Multa de Velocidade**. | O `if` verifica se a velocidade excedeu o limite de 80 km/h. Se houver excesso, calcula o valor da multa com base em R$ 5,00 por km excedido. |
| **Atividade05.py** | **Verificação de Par ou Ímpar**. | Utiliza o operador Módulo (`%`). A condição verifica se `numero % 2 == 0` para classificar o número. |
| **Atividade06.py** | **Fluxo Condicional Simples 1**. | Demonstra a reatribuição de `N` e `M` dependendo se `N - M > 0`. |
| **Atividade07.py** | **Fluxo Condicional Simples 2**. | Verifica se `N` é par. A condicional altera `M` (se par) ou `N` e `M` (se ímpar). |
| **Atividade08.py** | **Aprovação Simples (Soma das Notas)**. | O `if` classifica o aluno como **APROVADO** se a soma das notas bimestrais for maior que 6.0. |
| **Atividade09.py** | **Média Final (Aritmética ou Ponderada)**. | Se `media_bimestral >= 7.0`, a final é a média aritmética. Caso contrário, utiliza uma média ponderada com a `nota_exame`. |
| **Atividade10.py** | **Cálculo de Tarifa de Estacionamento**. | Diferencia a cobrança para tempo ≤ 60 minutos (tarifa fixa) ou tempo adicional (tarifa por fração de hora), usando `math.ceil`. |
| **Atividade11.py** | **Cálculo de Preço de Viagem (Desconto Progressivo)**. | Utiliza **`if/elif/else`** para aplicar tarifas decrescentes com base na distância da viagem (0.50, 0.45 ou 0.40 por km). |
| **Atividade12.py** | **Comparação de Altura em Relação à Média**. | Usa condicionais aninhadas: o `if/elif` externo verifica o sexo, e o interno compara a altura informada com a respectiva média populacional. |
